# Booking CRM Platform

This repository contains the Booking CRM Platform project.

## What I added
- Initial repository setup
- `.gitignore` for common ignores

## Next steps
- Add a remote and push: `git remote add origin <url>` then `git push -u origin main`
